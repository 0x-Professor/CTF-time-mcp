[project]
name = "ctf-times-mcp"
version = "1.0.0"
description = "MCP server for CTFtime.org - Access CTF events, teams, and rankings"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mcp[cli]>=1.25.0",
    "httpx>=0.27.0",
]

[project.scripts]
ctftime-mcp = "server:main"
